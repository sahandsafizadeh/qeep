version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - nakedret
    - wrapcheck

  settings:
    nakedret:
      max-func-lines: 0
    wrapcheck:
      ignore-sig-regexps:
        - '\.[a-z][A-Za-z0-9]*\(' # private functions
      report-internal-errors: true
